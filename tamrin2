function maxarr(arr){
    if (arr.length === 0) return null; 
  let max = arr[0]; 
  for (let i = 1; i < arr.length; i++) {
    if (arr[i] > max) {
      max = arr[i]; // 
    }
  }
  return max;
}
const numbers = [80, 27, 8, 32, 9,6];
console.log(maxarr(numbers));
